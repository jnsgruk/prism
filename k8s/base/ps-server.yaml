apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prism-workspaces
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prism-backups
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ps-server
spec:
  selector:
    app: ps-server
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ps-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ps-server
  template:
    metadata:
      labels:
        app: ps-server
    spec:
      serviceAccountName: ps-server
      securityContext:
        fsGroup: 1001
      containers:
        - name: ps-server
          image: prism/ps-server
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: DATABASE_URL
            - name: PS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: PS_SECRET_KEY
            - name: PORT
              value: "8080"
            - name: RESTATE_URL
              value: http://restate:8080
            - name: RESTATE_ADMIN_URL
              value: http://restate:9070
            - name: WORKSPACES_PATH
              value: /workspaces
            - name: WORKSPACES_CAPACITY_BYTES
              value: "53687091200" # 50Gi
            - name: BACKUPS_PATH
              value: /backups
            - name: BACKUP_IMAGE
              value: localhost:30500/prism_ps-backup:latest
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: RUST_LOG
              value: info
          volumeMounts:
            - name: workspaces
              mountPath: /workspaces
            - name: backups
              mountPath: /backups
          readinessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
      volumes:
        - name: workspaces
          persistentVolumeClaim:
            claimName: prism-workspaces
        - name: backups
          persistentVolumeClaim:
            claimName: prism-backups
