apiVersion: v1
kind: Service
metadata:
  name: ps-ingestion
spec:
  selector:
    app: ps-ingestion
  ports:
    - name: health
      port: 9080
      targetPort: 9080
    - name: restate
      port: 9081
      targetPort: 9081
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ps-ingestion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ps-ingestion
  template:
    metadata:
      labels:
        app: ps-ingestion
    spec:
      containers:
        - name: ps-ingestion
          image: prism/ps-ingestion
          ports:
            - containerPort: 9080
            - containerPort: 9081
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: DATABASE_URL
            - name: PS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: prism-secrets
                  key: PS_SECRET_KEY
            - name: RESTATE_URL
              value: http://restate:8080
            - name: RUST_LOG
              value: info
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
